# 自动发现、无账号、三语与作者文档

日期：2026-09-13。甲方确认继续自动发现，并要求取消登录、署名表单、分类、UI 简化、三语设置及各资源 README。

## 实际变化

- Anthropic 从固定 5 个 Skill 改为实际目录扫描：提交 34040c9c568585f6929bedeaad110ad08f079624，扫描 19、收录 12、跳过 7；目录合计 30 条真实资源及 2 个示例。
- 正常每 6 小时检查，失败 30 分钟后重试；暂停持久化、恢复立即检查，失败保留旧目录。无关提交不制造更新，确认移除项不重新出现。
- 取消账号及本机发布；五类统一带署名导出 GitHub 投稿文件。旧私有表保留但不公开，本机收藏、草稿、个人评分和语言持久化。
- 导航为发现/我的资源/设置；增加用途分类，保留简化横幅，减少重复徽章与说明。
- languages 下中英日共享键与占位符，设置即时切换，已知 API 错误共用语言文件。
- 插件自身 README、Skill 原始 SKILL.md、Slash 父插件文档、MCP 仓库文档和 Prompt 原文分别处理。显示来源、提交及版本匹配范围，失败可重试。
- Markdown 使用 [react-markdown 官方组件](https://github.com/remarkjs/react-markdown)，不执行原始 HTML，不自动加载远程图片，链接通过协议校验。

## 验证证据

首次门禁前置 61 项通过，宿主 11/12 通过。真实浏览器发现 URL 校验传入空 base 而误拒绝有效文档地址；补上失败回归测试并修复后，隔离宿主 12/12 通过，证据在 artifacts/dsh-integration/verify-mL4kGy。

完整门禁包含 62 项前置测试和 12 项真实宿主测试，合计 74。覆盖发现新名称、失败恢复、暂停恢复、目录修订、取消登录、旧库保留、五类署名导出、三语重载、个人状态、下载去重、README/Markdown、主题/字号/390px、引用附件和旧预览拒绝。

## 完成边界

自动发现仅接通 Anthropic Skill，其他来源仍更新指定条目。安装、加载、连接、Slash 执行、更多来源、公共统计、在线投稿同步和公共部署未完成。本轮未使用此前的 PAT，未推送仓库，未部署网站。分类不代表兼容认证，许可与散列检查不代表安全审计。
